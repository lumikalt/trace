RoundRobin(reqs : bits[N]) : bits[clog2(N)] <combines> {
    return prio(reqs)
}

module Top {
    in reqs : bits[4]
    out grant : bits[2] = 0

    rule pick {
        grant := RoundRobin(reqs)
    }
}
