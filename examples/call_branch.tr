Max(a : bits[8], b : bits[8]) : bits[8] <combines> {
    if a > b {
        return a
    } else {
        return b
    }
}

module Top {
    input a : bits[8]
    input b : bits[8]
    output result : bits[8] = 0

    rule compute {
        result := Max(a, b)
    }
}
