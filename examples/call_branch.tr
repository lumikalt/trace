Max(a : bits[8], b : bits[8]) : bits[8] <combines> {
    if a > b {
        return a
    } else {
        return b
    }
}

module Top {
    in a : bits[8]
    in b : bits[8]
    out result : bits[8] = 0

    rule compute {
        result := Max(a, b)
    }
}
