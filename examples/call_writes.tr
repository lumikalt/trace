module Top {
    in a : bits[8]
    out result : bits[8] = 0
    out v_out : bits[8] = 0

    Bump(x : bits[8]) : bits[8] <combines> {
        v_out := x
        return x + 1
    }

    rule compute {
        result := Bump(a)
    }
}
