Classify(x : bits[8]) : bits[8] <combines, fails> {
    (x != 0)?
    return x
}

module Top {
    in a : bits[8]
    out result : bits[8] = 0

    rule compute {
        result := Classify(a)
    }
}
