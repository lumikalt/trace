Avg(a : bits[8], b : bits[8]) : bits[8] <combines> {
    let sum = a + b
    return sum >> 1
}

module Top {
    in a : bits[8]
    in b : bits[8]
    out result : bits[8] = 0

    rule compute {
        result := Avg(a, b)
    }
}
