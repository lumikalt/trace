Avg(a : [8], b : [8]) : [8] <combines> {
    let sum = a + b
    return sum >> 1
}

module Top {
    in a : [8]
    in b : [8]
    out result : [8] = 0

    rule compute {
        result := Avg(a, b)
    }
}
