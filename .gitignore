# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# VSCode
/.vscode

# pre-commit
.pre-commit-config.yaml

# VS Code extension JS dependencies (vscode-languageclient et al.) —
# installed via `npm install` in editors/vscode, not checked in.
editors/vscode/node_modules


# Added by cargo

/target
